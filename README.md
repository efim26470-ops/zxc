# quietpress hero

A full-screen static hero built with React, TypeScript, Tailwind CSS, Vite, and lucide-react.

## Run locally

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
```

The CloudFront video is loaded directly in the browser and captured into canvas frames for the boomerang loop.
