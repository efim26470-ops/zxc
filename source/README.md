# Editable quietpress source

```bash
npm install
npm run dev
```

Production build:

```bash
npm run build
```

The output is written to `dist/`. The root of the parent repository already contains a compiled copy for branch-based GitHub Pages deployment.
