# Editable quietpress source

```bash
npm install
npm run dev
```

Production build:

```bash
npm run build
```

The output is written to `dist/`. The parent repository root contains a compiled copy for branch-based GitHub Pages deployment.

The Playback salon streams six CC0 tracks from the Internet Archive collection **New Midnight Cassette System** by Frank Edward Nora.
